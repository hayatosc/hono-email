{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "hono/jsx",
    "types": ["node"],
    // Drop the library's dev path mappings inherited from the root tsconfig so
    // `hono-email` resolves to the built package (dist) via node_modules.
    "paths": {}
  },
  "include": ["src"]
}
